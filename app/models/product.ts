export class Product {
  imageSets: Array<{thumb: string, large: string}>;

  constructor(
    id: string,
    title: string,
    brand: string,
    price: string,
    percentageSaved: number,
    image: string){};
}